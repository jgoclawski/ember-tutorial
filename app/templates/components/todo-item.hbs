<li class="{{if todo.isCompleted 'completed'}}">
  <input type="checkbox" class="toggle" checked={{todo.isCompleted}} onclick={{action "complete" value="target.checked"}} />
  <label>{{todo.title}}</label><button class="destroy"></button>
</li>
